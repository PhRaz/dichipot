{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajout d'un nouveau participant pour {{ event.name }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Save</button>
    {{ form_end(form) }}
    <nav>
        <a href="{{ path('home') }}">Home</a>
        <a href="{{ path('user_list') }}">Liste des utilisateurs</a>
        <a href="{{ path('event_list', {'userId': administrator.id}) }}">Liste des évènements de {{ administrator.name }}</a>
    </nav>
{% endblock body %}
