{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('home') }}">retour vers menu</a>
    <h1>Création un nouvel évènement pour {{ user.name }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Save</button>
    {{ form_end(form) }}
{% endblock body %}
