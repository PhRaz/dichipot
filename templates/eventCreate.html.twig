{% extends 'base.html.twig' %}

{% block body %}
    <h2>Evènement pour {{ user.name }}</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Save</button>
    {{ form_end(form) }}
{% endblock body %}
